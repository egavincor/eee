% Generated by roxygen2: do not edit by hand
% Please edit documentation in
%   R/two_variable_error_ratio_specified_exact_statistics.R
\name{two_variable_exact_CI_formula}
\alias{two_variable_exact_CI_formula}
\title{Calculate Exact Confidence Interval for a Two-Variable Relationship via Formula}
\usage{
two_variable_exact_CI_formula(
  formula,
  data = NULL,
  lambda,
  slope_hypothesis = NA,
  CI_coefficient = 0.95,
  print_citation_info = TRUE,
  sql_round_to_zero_threshold = 1e-10,
  lostDoF = 2,
  debugArg = FALSE,
  checkArg = TRUE
)
}
\arguments{
\item{formula}{A two-sided formula of the form \code{y ~ x}, where \code{y}
is the response variable and \code{x} is the predictor variable. Both
variables must be numeric vectors of the same length with no missing
values. Multivariate formulas (e.g. \code{y ~ x1 + x2}) are not
supported.}

\item{data}{An optional data frame (or environment) in which to evaluate
\code{formula}. If \code{NULL} (the default), variables are looked up in
the calling environment.}

\item{lambda}{\code{numeric} lambda
is the ratio of the error in the y-axis variable to that in the x-axis
variable.}

\item{slope_hypothesis}{\code{numeric} slope (if any) to test as null hypothesis.}

\item{CI_coefficient}{\code{numeric} defaults to 0.95, \% of time not to reject the
null hypothesis assuming it is true.}

\item{print_citation_info}{\code{logical} defaults to \code{TRUE}, which means it
prints citation information for this function. Suggested citations is/are the
works that contribute to at least some output or calculation of the
function in some way and deserve citation.}

\item{sql_round_to_zero_threshold}{\code{numeric} if the square root of lambda
is less than this threshold, then lambda is rounded
to zero as appropriate and so the answer should be identical to as if
zero was given as \code{lambda}/\code{square_root_of_lambda}}

\item{lostDoF}{\code{numeric} defaults to 2, I have never tried changing
this value, but you can change it IF you have a compelling reason.
Most significance tests require subtracting 2
degrees of freedom from the sample size.}

\item{debugArg}{\code{logical} defaults to \code{FALSE}, \code{TRUE} prints internal
functional information (if any) to terminal.}

\item{checkArg}{\code{logical} defaults to \code{TRUE}, which checks arguments for
some basic formatting mistakes.}
}
\value{
A named \code{list} with the following elements:
\describe{
\item{slope_cartesian}{see \code{\link[=two_variable_exact_CI]{two_variable_exact_CI()}}}
\item{slope_polar}{see \code{\link[=two_variable_exact_CI]{two_variable_exact_CI()}}}
\item{clockwise_CI_cartesian}{see \code{\link[=two_variable_exact_CI]{two_variable_exact_CI()}}}
\item{clockwise_CI_polar}{see \code{\link[=two_variable_exact_CI]{two_variable_exact_CI()}}}
\item{counterclockwise_CI_cartesian}{see \code{\link[=two_variable_exact_CI]{two_variable_exact_CI()}}}
\item{counterclockwise_CI_polar}{see \code{\link[=two_variable_exact_CI]{two_variable_exact_CI()}}}
\item{CI_polar_width}{see \code{\link[=two_variable_exact_CI]{two_variable_exact_CI()}}}
\item{x_reliability_ratio}{see \code{\link[=two_variable_exact_CI]{two_variable_exact_CI()}}}
\item{slope_test_p_value}{see \code{\link[=two_variable_exact_CI]{two_variable_exact_CI()}}}
\item{calculationTerms}{see \code{\link[=two_variable_exact_CI]{two_variable_exact_CI()}}.}
\item{givenArguments}{see \code{\link[=two_variable_exact_CI]{two_variable_exact_CI()}}}
\item{x_error_variance}{estimate of variance of x-axis variable's error,
calculated as \code{(1-x_reliability_ratio)*var(x)}, where
\code{var(x)} is calculated by dividing by \code{(n-1)} instead of \code{n}}
\item{y_error_variance}{estimate of variance of y-axis variable's error}
\item{y_intercept}{point where line of best fit crosses y-axis}
\item{x_intercept}{point where line of best fit crosses x-axis}
}
}
\description{
Accepts a
standard R formula and optionally a data frame, extracting the response and
predictor variables automatically before computing the specified
error-in-variable regression
}
\examples{
set.seed(0)
df <- data.frame(y = rnorm(20), x = rnorm(20))

# Using a data frame
result <- two_variable_exact_CI_formula(
  formula             = y ~ x,
  data                = df,
  lambda = 1,
  print_citation_info = FALSE
)
result$slope_cartesian

# Using variables from the calling environment
y <- rnorm(20); x <- rnorm(20)
result2 <- two_variable_exact_CI_formula(
  formula             = y ~ x,
  lambda = 1,
  print_citation_info = FALSE
)
result2$slope_cartesian
}
